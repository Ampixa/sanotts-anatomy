/**
 * HeroView — chapter 0's right view: the interactive whole-pipeline overview.
 */
import { Overview3D } from "./Overview3D";

export function HeroView() {
  return <Overview3D focusChapter={null} />;
}
